# Glossary

| Term | Meaning | Notes |
|------|---------|-------|
| AC | Acceptance Criteria | Test names should map to AC numbers |
| Model set | Sample domain object | A named collection of models |
| Collection root | The URL of a collection itself, e.g. `/api/model-sets` | Registered as `@router.get("")` under a router prefix |
