---
description: Convert an approved PrismSpec spec.md into an AC-traced plan.md
---

Use `prismspec/skills/prismspec-planning/SKILL.md`.

Run:

```bash
bash prismspec/bin/guide.sh $ARGUMENTS --from=planning --json
```
