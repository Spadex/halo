---
description: Execute the next PrismSpec implementation slice with evidence
---

Use `prismspec/skills/prismspec-implementation/SKILL.md`.

Run:

```bash
bash prismspec/bin/guide.sh $ARGUMENTS --from=implementation --json
```
